/* exported capitalize */
function capitalize(word) {
  return word[0].toUpperCase() + word.slice(1).toLowerCase();
}
// lowercase word at the index 1
// uppercase word at the index 0
// concatenate the values and return from function
